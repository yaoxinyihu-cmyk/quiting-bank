<!-- 评估表单页 -->
<view class="page">

<!-- 背景头部 -->
<view class="header-bg">
  <text class="header-title">烟草依赖程度测试量表</text>
  <text class="header-sub">请您如实填写以下资料，以便我们更精准地掌握您的吸烟情况</text>
</view>

<view class="card">
  <image src="../../assets/images/logo.png" class="logo" mode="aspectFit"></image>

  <!-- 表单列表 -->
  <view class="form">
    <!-- 问题1 -->
    <view class="form-item">
      <text class="label">您早晨起来多长时间吸一支烟?</text>
      <picker mode="selector" range="{{timeOptions}}" value="{{timeIndex}}" bindchange="onTimeChange">
        <view class="picker">
          <text>{{ timeIndexText }}</text>
          <text class="arrow">▾</text>
        </view>
      </picker>
    </view>

  <!-- 问题2 -->
  <view class="form-item">
    <text class="label">您是否在很多吸烟场所很难控制吸烟的需求?</text>
    <radio-group bindchange="onQ2Change">
      <label class="radio-item"><radio value="1" checked="{{q2==='1'}}" /> 是</label>
      <label class="radio-item"><radio value="0" checked="{{q2==='0'}}" /> 否</label>
    </radio-group>
  </view>

  <!-- 问题3 -->
  <view class="form-item">
    <text class="label">您认为哪一只烟最不愿意放弃?</text>
    <picker mode="selector" range="{{whichOptions}}" value="{{whichIndex}}" bindchange="onWhichChange">
      <view class="picker">
        <text>{{ whichIndexText }}</text>
        <text class="arrow">▾</text>
      </view>
    </picker>
  </view>

  <!-- 问题4 -->
  <view class="form-item">
    <text class="label">您早晨醒来后第一个小时是否比其他时间吸烟多?</text>
    <radio-group bindchange="onQ4Change">
      <label class="radio-item"><radio value="1" checked="{{q4==='1'}}" /> 是</label>
      <label class="radio-item"><radio value="0" checked="{{q4==='0'}}" /> 否</label>
    </radio-group>
  </view>

  <!-- 问题5 -->
  <view class="form-item">
    <text class="label">您卧病在床是仍旧吸烟吗?</text>
    <radio-group bindchange="onQ5Change">
      <label class="radio-item"><radio value="1" checked="{{q5==='1'}}" /> 是</label>
      <label class="radio-item"><radio value="0" checked="{{q5==='0'}}" /> 否</label>
      <label class="radio-item"><radio value="2" checked="{{q5==='2'}}" /> 暂无卧病在床经历</label>
    </radio-group>
  </view>
</view>
</view>

<!-- 确认按钮 -->
<view class="footer">
  <button class="confirm-btn" bindtap="onConfirm">确认信息开始测试</button>
</view>
</view>
